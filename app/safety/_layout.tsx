import { Stack } from "expo-router";

/**
 * Safety detail stack layout.
 * @returns Safety stack
 */
export default function SafetyLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
