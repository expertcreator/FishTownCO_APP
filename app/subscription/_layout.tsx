import { Stack } from "expo-router";

/**
 * Subscription stack layout.
 * @returns Subscription stack
 */
export default function SubscriptionLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
